// function addTags(content, tags) {
//   const items = JSON.parse(localStorage.getItem('tagged-items') || [];
//   items.push({ content, tags });
//   localStorage.setItem('tagged-items', JSON.stringify(items));
// }

// function filterByTag(tag) {
//   const items = JSON.parse(localStorage.getItem('tagged-items') || [];
//   return items.filter(item => item.tags.includes(tag));
// }